# Processed Data

Large processed datasets are not committed to this repository.

To run `scripts/final_analysis.ipynb`, place the processed tract-level checkpoint here:

```text
data/processed/tract_indicators_full.gpkg
```

The notebook writes derived tables to `data/revised/` and updates web-map assets in `src/atlas/assets/`.

